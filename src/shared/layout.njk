<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {{ site.brand }}{% else %}{{ site.brand }}{% endif %}</title>
  <meta name="description" content="{{ description | default(site.brand ~ ' services for your home.') }}">
  <link rel="canonical" href="{{ canonical | default(site.domain + page.url) }}">
  <link rel="stylesheet" href="/assets/styles.css">
  <script defer src="/assets/js/reviews.js"></script>
</head>
<body>
  {% include "header.njk" %}
  <main>
    {{ content | safe }}
  </main>
  {% include "footer.njk" %}
</body>
</html>
